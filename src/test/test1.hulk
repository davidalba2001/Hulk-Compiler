type Point {
    
    x = 5;
    y = 6;
    
    getX() => 5;
    getY() => 5;

    setX(x) => 5;
    setY(y) => 5;
}

function count_when(numbers: Number, filter) {
    (numbers is filter) > 3;
}

protocol Hashable {
    hash(): Number;
}


{
    3+2+1;
    let a = 2 in {
        a + 4 as number;
    };

    {
        while(true){
            3+1;
        };
        for(x in range(0, 10)) 
        {
            111SSS
        };
        yo_soy := [x || x in range(1, 2)];
        agua := new Pinga(flacida);
        aceite.agua(yo_soy_una_guagua);
        yo_soy[una_guagua];
        3+1;
        2+7-1;
        {
            "habla" @ "matador";
        }
    };

}